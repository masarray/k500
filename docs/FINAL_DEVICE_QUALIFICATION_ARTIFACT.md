# Final qualification artifact

Use the `K500-Final-Device-Qualification` GitHub Actions artifact from this exact PR/commit for physical testing. The embedded performance JSON records the configured Git commit so test evidence cannot be confused with another build.
