# P3 Stacking Note

P3 is intentionally stacked on `architecture/p2-deterministic-transaction-scheduler` while PR #74 is still open. After P2 merges into `main`, retarget the P3 pull request to `main` without changing P3 semantics.
